
import tas.utils.HelpTarget

class HelpImpl extends HelpTarget("prediction searcher",
                                  List("SearchPrediction - performs parameter probing, searches for prediction combinations."))

object Help extends HelpImpl
object help extends HelpImpl
