
import tas.utils.HelpTarget

class HelpImpl extends HelpTarget("periods statistics collector",
                                  List("CollectStatistics - performs parameter probing, collects statistics based on history data."))

object Help extends HelpImpl
object help extends HelpImpl
